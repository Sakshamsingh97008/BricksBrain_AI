# Optional. If set, the Interior Design feature calls OpenAI's image API for
# real AI-generated room renders. If left blank, a curated inspiration gallery
# + design tips are shown instead, so the feature still works with no key.
OPENAI_API_KEY=
